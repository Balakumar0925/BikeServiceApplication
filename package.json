{
  "name": "project1",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:app": "cd client && npm build",
    "install": "npm build && npm run-script build:app",
    "server": "node app.js",
    "start": "concurrently  \"npm run-script server\" \"npm run-script install\"",
    "postinstall": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run-script build && npm start",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run-script build && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^6.2.0",
    "express": "^4.17.1",
    "mongodb": "^3.6.6",
    "nodemailer": "^6.6.0"
  }
}
