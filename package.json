{
  "name": "project1",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:client": "cd frontend/bike-reactjs/ && npm install && npm run build",
    "build:server": "npm install",
    "start:client": "npm start --prefix frontend/bike-reactjs",
    "start:server": "node app.js",
    "start:both": "concurrently \"node app.js\" \"npm start --prefix frontend/bike-reactjs\"",
    "server": "npm install && node app.js",
    "client": "npm run-script build && npm start --prefix frontend/bike-reactjs",
    "start": "npm run build:client && npm run build:server && npm run start:both ",
    "heroku-postbuild": "cd frontend/bike-reactjs && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^6.2.0",
    "express": "^4.17.1",
    "mongodb": "^3.6.6",
    "nodemailer": "^6.6.0"
  }
}
